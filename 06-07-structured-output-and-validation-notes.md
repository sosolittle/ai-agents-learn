# 第 6–7 课详解：结构化输出与输入输出校验

> 学习笔记，基于 `01-basics/6.structured-output` 与 `01-basics/7.input-output-validation`。
> 两课是同一个问题的两半：**第 6 课讲"怎么向模型要结构"（契约设计），第 7 课讲"怎么在边界上验收结构"（边界执法）**。

---

## 背景故事：一个贯穿两课的问题

先建立一个具体场景。假设你在做一个小工具：爬取招聘网站，把自然语言写的职位描述，变成你数据库里的一行记录：

```
输入（一段人话）:
"Acme Corp 正在招聘！我们希望找一位高级全栈工程师加入伦敦团队…
5 年以上 React 和 Node.js 经验，有 PostgreSQL 使用经验，最好也会一些 TypeScript…
薪资：年薪 £85,000 到 £110,000…"

输出（一个对象）:
{
  job_title: "高级全栈工程师",
  company: "Acme Corp",
  ...
}
```

为什么必须要对象？因为下游全是程序：数据库要字段名稳定的列、筛选页要按 `seniority_level` 过滤、排序要按 `salary_range.min` 排。**自由文本只能给人读，结构化数据才能给程序用。**

这里藏着一个初学者必须先想通的底层事实：

> **模型从头到尾只是在"生成文本"。** 它没有"返回一个对象"的概念——它是把文本一个 token 一个 token 吐出来的字符串生成器。所谓"结构化输出"，不是模型换了种工作方式，而是我们想办法让**它生成的字符串恰好是能被 `JSON.parse` 解析、且形状符合预期的 JSON**。

记住这句话，两课的所有设计都是围绕它展开的：第 6 课提高"字符串恰好合格"的概率，第 7 课保证"不合格的字符串绝对过不去"。

---

## 第一部分：第 6 课 —— 怎么向模型"要"结构

### 1.1 数据契约：先设计输出，再写提示词

两课共用的第一步是定义 `JobPosting`（`6.structured-output/index.ts:26-33`）：

```ts
interface JobPosting {
  job_title: string;
  company: string;
  location: string;
  salary_range: { min: number; max: number; currency: string } | null;
  required_skills: string[];
  seniority_level: "junior" | "mid" | "senior" | "lead" | "unknown";
}
```

这种定义有个名字：**数据契约（data contract）**——上游（模型）承诺交出什么形状，下游（你的业务代码）依赖什么形状。逐字段看它的设计心思：

**`salary_range: {...} | null` —— 给模型"承认没有"的出口。**

职位描述可能根本不写薪资。如果契约里这个字段必须是对象，模型就被逼着在两种坏事里选：编一个数字（幻觉），或者返回 `undefined` 破坏契约。允许 `null` 等于告诉模型："没有就说没有，别猜。"这是 agent 开发里反复出现的主题——**契约设计得好，模型撒谎的机会就少**。

**`seniority_level` 的枚举里放 `"unknown"` —— 兜底值。**

"高级全栈工程师"能映射到 `senior`，但"招聘一位小伙伴加入团队"映射到哪个？没有 `unknown` 这个出口，模型只能硬凑一个。设计枚举时的通用姿势：**枚举值 + 一个"不确定"出口**。

**一个小练习（作者没写进代码，但值得你跑）**：这份契约其实有歧义——"最好也会一些 TypeScript"和"加分项：Redis、Docker"算不算 `required_skills`？契约没说，模型每次只能自己拍脑袋。你跑那个 10 次循环时，除了看格式稳不稳，还可以看 `required_skills` 的值飘不飘——**歧义的契约会产生不稳定的输出**，这是用真实输出反推契约设计缺陷的好方法。

### 1.2 必须搞懂的概念：编译期 vs 运行时

`index.ts:34-36` 的注释是这两课最重要的一句话：

> JobPosting 是 TypeScript 类型，只在**编译期**帮助你。tools.parameters 是 JSON Schema，会发给模型，属于**运行时**约束。

初学者经常在这里糊涂，把它彻底讲开：

- **编译期**：你写 `.ts`，要编译成 `.js` 才能跑。TypeScript 在这一步检查类型、报红、提示你。但检查完之后呢？——**类型信息全部被扔掉**。这个过程叫**类型擦除（type erasure）**。跑在 Node 里的最终代码是纯 JavaScript，里面已经没有任何 `interface JobPosting` 的痕迹了。
- **运行时**：程序真正执行的时刻。此时能依赖的只有"代码里实际执行的逻辑"——`if` 判断、`JSON.parse`、Zod 校验这些**真的会跑的代码**。

一个直白的类比：TS 类型是**图纸**，运行时对象是**成品**。图纸指导施工，但成品不会自动长出"对照图纸自查"的能力——你得安排质检员（第 7 课的 Zod）。

### 1.3 `as JobPosting` 的真相：声称，不是检查

现在你能看懂第 6 课最危险的一行了（`index.ts:142`）：

```ts
return JSON.parse(message) as JobPosting;
```

分两步理解：

1. `JSON.parse` 收到的必须是**字符串**（模型返回的 `message.content` 就是字符串），返回值的类型是 `any`——TypeScript 对它一无所知。
2. `as JobPosting` 做的事情仅仅是：**告诉编译器"我声称它是这个形状，你按这个形状给我补全提示吧"**。它不生成任何检查代码。运行时它什么都没做，零成本，零保护。

所以如果模型手滑返回了 `{"job_titel": "工程师"}`（拼错字段名），这行代码照样通过编译。后面代码里 `result.job_title` 拿到 `undefined`，然后可能在离出错现场很远的地方炸掉——比如 `.length` 报错，你回头查半天才发现是两小时前那个解析点的字段名错了。**运行时错误的最坏形态不是"崩溃"，而是"在离病因很远的地方崩溃"**，`as` 盲断言正是这种远距离 bug 的头号制造者。

### 1.4 三种"要结构"的方式：从软到硬

文件夹里两个版本的对比，恰好展示了约束强度的光谱（`index.ts` 是提示词版，`index副本.ts` 是 function calling 原版）。

**方式 A：提示词约定（当前 `index.ts` 的做法）**

把格式要求全部写进 prompt：任务说明 + 约束清单（"只返回 JSON，不要输出任何解释"、"没有薪资返回 null"、枚举列表）+ 一段 JSON 骨架。这是第 4 课模板思想的直接应用。

它的本质是**软约束**。回到开头那句话：模型是概率性的 token 生成器，"只返回 JSON"只是把"输出 JSON"这条路径的概率推得很高，**不是 100%**。真实失败形态包括：

- 模型"贴心"地在 JSON 外面套 ```` ```json … ``` ```` 代码围栏（内容全对，`JSON.parse` 照样挂）；
- 返回 JSON 前多一句"好的，以下是提取结果："；
- 长回答被 `max_tokens` 截断，JSON 只有一半（呼应第 1/3 课的 `finish_reason: "length"`）；
- 漏字段、把布尔写成字符串、自创枚举值。

**方式 B：function calling 当"输出模具"（`index副本.ts` 的原版做法）**

```ts
tools: [{ type: "function", function: { name: "extract_job_posting", parameters: { /* JSON Schema */ } } }],
tool_choice: { type: "function", function: { name: "extract_job_posting" } },
```

这里有个非常巧妙、也最容易被初学者误解的观念：**这个 function 从头到尾不会被执行**。注释原话："我们把它当作结构化输出通道：模型必须填好 arguments。"

也就是说：function calling 本来是为"让模型调用你的后端函数"设计的（第 2 课的正题），但这里**借用工具的参数框当输出的模具**——模型不是在"写一篇文章"，而是在"填一张你印好的表"，且 `tool_choice` 强制它必须填这张表。因为各大模型在训练时专门优化过"填参数"这个行为，它守格式的概率远高于提示词。

但两个常见误解要提前拆掉：

- **误解一："function calling = 模型执行了函数"。** 错。模型只做了填表（生成一段 arguments 字符串），**执行永远发生在你的代码里**。事实上模型根本没有执行任何东西的能力，它只能"申请"你去做。
- **误解二："用了工具调用就不用校验了"。** 错。`toolCall.function.arguments` 仍然是**一个 JSON 字符串**，你仍然要 `JSON.parse`；边缘情况下它仍可能违反 schema。原版代码里那行 `if (!toolCall) throw new Error("模型没有返回 tool call")`（被注释保留在 `index.ts:132-133`）就是作者承认这一点：即使强制了 `tool_choice`，工程代码也要检查异常情况。

`index.ts:20-23` 还留了一个供应商适配的现实细节：DeepSeek 的部分思考模型会拒绝 `tool_choice` 参数，所以检测到 DeepSeek 就降级（只传 `tools`，靠提示词补一句"请调用某某工具"）。**约束强度不只取决于方法，还取决于供应商支持**——这也是为什么跨供应商的 agent 通常把"获取结构"和"验收结构"分开处理：前者各家不同，后者永远归你的代码管。

**方式 C（补充，代码里没有）：`response_format` 严格模式**

OpenAI 后来推出的 Structured Outputs（`response_format: { type: "json_schema" }`）在 API 层面承诺输出符合 schema，是目前最硬的保证。可以理解为"方式 B 的官方进化版"。但即便用它，第 7 课的输出校验依然建议保留——**验收是你的责任，不能外包给任何单一供应商的承诺**。

| | A. 提示词 | B. function calling | C. response_format 严格模式 |
|---|---|---|---|
| 约束强度 | 弱（概率推高） | 中（训练专项优化 + 强制填表） | 强（API 承诺） |
| 供应商兼容 | 全部 | 大部分，细节有差异 | 少数 |
| 还需要 parse + 校验吗 | 必须 | 必须（arguments 是字符串） | 建议保留 |
| 成本 | 无额外 | schema 占输入 token | schema 占输入 token |

### 1.5 10 次循环：一个朴素的"可靠性实验"

`main()` 里 `for (let i = 0; i < 10; i++)` 是作者自己加的实验：同一段输入抽 10 次，观察格式和值稳不稳。这其实是在手工做**可靠性评估**——生产 agent 团队会把它自动化成评测集（跑 100 条样本统计通过率）。顺便注意：这里**没设 `temperature`**（默认偏随机），对比第 7 课的 `temperature: 0`，两课对稳定性的态度差一个参数。

再推演一个具体故障：循环里 `JSON.parse` 裸奔，只要 10 次里有 1 次模型套了围栏，异常沿 `await` 一路抛出，**整个程序当场终止**，剩下 9 次不跑了。概率性的失败 × 脆弱的代码结构 = 偶发且难复现的线上事故。带着这个隐患进入第 7 课。

---

## 第二部分：第 7 课 —— 怎么在边界上"验收"

### 2.1 核心心智模型：把模型当成一个不可靠的外部服务

第 7 课的 README 有一句提纲挈领的话：**模型负责生成内容，应用代码负责判断这些内容能不能被接受和继续使用。**

给初学者一个更好用的换法：**像对待第三方 API 一样对待模型。** 你调用一个支付接口，返回值会不检查就往数据库写吗？不会。那模型这个"接口"的概率性输出，凭什么不检查？它甚至比支付接口更不可靠——支付接口挂了会报错，模型"挂了"是**微笑着返回一段看起来很像答案的错误内容**。

于是整个系统的形状是一个三明治，**两个信任边界夹着一个不可靠组件**：

```
用户输入                                              业务逻辑
    │                                                    ▲
    ▼                                                    │
[边界1: 输入校验] ──→ 模型（生成文本）──→ [边界2: 输出校验]
 UserTextSchema          ⚠️ 不可靠          ① JSON.parse（语法层）
 trim/1~500字                              ② Zod（语义层）
```

注释里的原话："这不是不信任模型，而是把不确定性关在边界外。"像安检：不是怀疑每个乘客，而是制度上不允许未检查的东西进入。

**为什么输入侧也要拦？** 三个理由：

1. **省钱省时间**：空字符串拦下来，不用花 token 让模型告诉你"这是空的"；
2. **快速失败**：明显不合法的输入（超长、全空格）在边界处一行日志打发掉，控制流干净；
3. **注入卫生**：用户文本里可能混着"忽略之前的所有指令"这类内容（被注释的原版演示里就有这么一条注入文本）。它们应该在边界被当作**数据**对待，而不是放进系统指令的位置。

### 2.2 Zod：让"运行时检查"和"编译期类型"变成一份定义

第 6 课要维护两份形状（TS interface + 发给模型的 JSON Schema），手工保持同步。第 7 课引入 Zod 后只剩一份：

```ts
const AnalysisSchema = z.object({
  summary: z.string(),
  sentiment: z.enum(["positive", "neutral", "negative"]),
  actionRequired: z.boolean(),
});

type Analysis = z.infer<typeof AnalysisSchema>;   // ← 类型从 schema 推导出来
```

`z.infer` 的意思是：**schema 是唯一事实来源，TS 类型是它的影子**。改 schema，类型自动跟着变——从此不存在"类型说有这个字段、运行时却没有"的双源漂移。

三个字段的构造也值得看：`sentiment` 用枚举限死三个值。为什么？想象下游要写 `if (result.sentiment === "positive")`——如果模型自由发挥返回 `"还行"`、`"happy"`、`"总体不错"`，这个 `if` 永远不命中，**而且不报任何错**，功能悄悄失效。枚举就是给下游分支语句上保险。

**`safeParse` vs `parse`**——Zod 的两种校验调用：

- `parse` 不合格就抛异常；
- `safeParse` 永不抛异常，返回 `{ success: true, data }` 或 `{ success: false, error }`。

代码统一用 `safeParse`（`7.input-output-validation/index.ts:46, 77`），注释说原因是"控制流更清楚"。这背后是一个值得记住的 JS 工程思想：**把错误当返回值（结果对象），而不是当例外（异常）**。校验失败是**预期内的正常分支**，不是意外事故，用 `if (!result.success) {...}` 写出来比 try/catch 包裹更直白。

### 2.3 `parseJsonObject`：一个微型的"结果对象"模式

```ts
function parseJsonObject(raw: string) {
  try {
    return { ok: true as const, value: JSON.parse(raw) };
  } catch (error) {
    return { ok: false as const, reason: `JSON.parse failed: ${(error as Error).message}` };
  }
}
```

逐个拆：

- **try/catch 在这里、且只在这里出现**：`JSON.parse` 是个"失败靠抛异常"的旧式 API，这个函数把它**翻译**成"失败靠返回值"的风格，下游就不用再写 try/catch 了。边界翻译，一次到位。
- **返回两种形状**：成功 `{ ok: true, value }`，失败 `{ ok: false, reason }`。`as const` 是让 TS 把 `"true"` 识别为字面量类型——之后 `if (!parsed.ok)` 会让 TypeScript 自动收窄类型，在成功分支里 `parsed.value` 可用、失败分支里 `parsed.reason` 可用。这种"一个标签字段区分两种结果"的写法叫**可辨识联合（discriminated union）**，你在后面模块的 types.ts 里会大量见到它。

对比第 6 课：同样的 `JSON.parse`，那边裸奔（一次失败炸全场），这边包好（失败变成可检查的普通返回值）。**两课对同一个 API 的不同待遇，就是工程成熟度的差距。**

### 2.4 输出校验为什么必须两步

`validateModelOutput`（`index.ts:64-86`）的流程：

```
模型返回的字符串
   │
   ▼
① JSON.parse ─── 语法层："这是一段语法合法的 JSON 吗？"
   │ 通过
   ▼
② Zod safeParse ─ 语义层："这个 JSON 是我要的形状吗？（字段全不全、类型对不对、枚举值合法吗）"
   │ 通过
   ▼
业务代码拿到类型安全的 Analysis
```

**两步各拦一类问题，缺一不可**：第①步拦"根本不是 JSON"（纯文字、围栏、截断），第②步拦"是 JSON 但不是我的 JSON"（漏字段、类型错、枚举越界）。只做①，一堆"合法的垃圾"长驱直入；只做②——不行，Zod 还没上场 `JSON.parse` 就先抛了。

当前 `main()`（`index.ts:176-232`）就是为第②步准备的靶场：**8 个手写的失败样本**，不调模型直接喂给校验函数。逐个讲透它们——每一个都是真实世界出现过的尸体：

| # | 用例 | 死在哪层 | 背后的真实故事 |
|---|---|---|---|
| 1 | `""` 空字符串 | ① parse 抛错 | 模型走了工具调用路线，`content` 为空（第 1 课埋的伏笔：只看 content 不看 finish_reason 的后果） |
| 2 | `"产品很好，但配置过程比较困难。"` 纯文字 | ① | 第 6 课提示词版最常见的失败：模型多嘴加了一句人话 |
| 3 | `{"summary":"产品很好`（截断） | ① | `max_tokens` 给小了，`finish_reason: "length"`，JSON 只剩前半截 |
| 4 | ```` ```json{...}``` ```` 带围栏 | ① | 最冤的一种：**内容完全正确**，只是被 Markdown 围栏包了一层，`JSON.parse` 照样挂。生产解法是先剥围栏再解析 |
| 5 | 漏了 `actionRequired` 字段 | ② Zod 报缺字段 | schema 里没标 optional 的字段，模型觉得"用不上"就省了 |
| 6 | `actionRequired: "false"`（字符串冒充布尔） | ② Zod 报类型错 | 见下面单独深挖——最阴险的一个 |
| 7 | `sentiment: "mixed"` | ② Zod 报枚举越界 | 模型觉得两个情绪都说得对，自创了第三种值 |
| 8 | `[]`（顶层数组） | ② Zod 报顶层类型错 | 模型理解成"返回一个列表"，每项才是对象 |

第 1~4 个证明"**能解析成 JSON**"这一步不可省，第 5~8 个证明"**符合业务契约**"是另一回事。这就是"合法的 JSON"和"我要的 JSON"之间的鸿沟——第 6 课的 `as` 断言对这道鸿沟完全失明。

### 2.5 深挖 `"false"` 字符串陷阱：为什么它是最危险的一个

用例 6 值得单独开一节，因为它演示了一类**不崩溃的 bug**——比崩溃危险得多的东西。

JavaScript 判断真假的规则（truthy/falsy）：`false`、`0`、`""`、`null`、`undefined`、`NaN` 是假值，**其他一切全是真值**——包括非空字符串 `"false"`。

于是：

```js
const result = { summary: "...", sentiment: "positive", actionRequired: "false" };
// 第 6 课的 as 断言放行了这个对象（编译期不查运行时）

if (result.actionRequired) {
  // "false" 是非空字符串 → 真值 → 这里的代码会执行！
  escalateToHumanAgent();   // 每条普通反馈都升级成了人工工单
}
```

体会一下这个 bug 的性质：**没有任何报错**。程序正常跑，日志正常打，只是业务逻辑悄悄全错——所有"不需要跟进"的反馈都被当成"需要跟进"。等你从工单量暴涨倒查回来，根因是模型某天开始把布尔写成字符串格式。Zod 在边界上就把这类东西拦成显式失败：宁可响亮地死，不要安静地错。

### 2.6 校验之后怎么办？（代码没写、但你必须开始想的问题）

两课的代码止步于"打印失败原因"。真实的工程决策在校验**之后**才开始，常见四种：

1. **重试**：最常用。且进阶做法是把失败原因喂回给模型再试一次——"你上次返回的 JSON 中 `actionRequired` 应为布尔值，请修正后重新输出"。这是**自我修正（self-correction）**模式的萌芽，比盲目重试省 token、收敛快；
2. **降级**：换个更便宜/更稳的路径（比如规则引擎兜底分类）；
3. **拒绝**：明确告诉上游"这条处理不了"，带着原因；
4. **人工介入**：高价值场景转人审。

一个可以立刻做的改进练习：当前 `validateModelOutput` 失败时返回 `null`，调用方**只知道失败、不知道为什么**（原因只打在了控制台）。把它也改成结果对象 `{ ok: true, data } | { ok: false, stage: "parse" | "schema", reason }`——下游才能区分"该重试"和"该报警"。这个小改造就是可辨识联合的实战。

### 2.7 当前 main() 的改动本身就是一个观念：确定性测试

原版 `main`（现在注释着）是调真模型演示"输入被拒/输出被拦"。作者改成 8 条**手写的死样本**直接喂校验函数——不花一分钱、一秒跑完、**每次结果完全一样**。

这个改动预演了 11/12 模块的测试哲学（也是现代 agent 团队的共识）：**测试永远不调模型**。原因很实际：模型输出是随机的，拿它当测试基准，测试会"偶尔挂"——而"偶尔挂"的测试比没有测试更糟，因为大家会习惯性忽略红色。所以要测的分成两堆：**确定性的部分（校验、解析、边界逻辑）用死样本测**；**模型能力的部分用评测集（离线统计通过率）**。混在一起就两头都做不好。

---

## 第三部分：把两课合成一个动作

完整的心智图，也是你以后写任何 agent 输出处理代码的固定手势：

```
                        ┌────────────── 第 6 课的领地 ──────────────┐
设计契约（字段/可空/枚举/兜底值）
        │
        ▼
选择约束方式（提示词 / 工具模具 / response_format）── 只提高合格率，不提供保证
        │
        └──────────────┬───────────────────────────────────────────┘
                       ▼
                模型生成一段字符串
                       │
        ┌──────────────┴────────────── 第 7 课的领地 ──────────────┐
        ▼
① JSON.parse（try/catch 包成结果对象）── 语法关
        │
        ▼
② Zod safeParse（schema 即类型，z.infer 单一来源）── 语义关
        │
        ▼
类型安全的对象 → 业务代码 / 重试（可带失败原因回喂）/ 降级 / 拒绝 / 人审
        └───────────────────────────────────────────────────────────┘
```

两课各一句话：

- **第 6 课**：当模型输出要变成程序输入时，设计一个明确的数据契约，并选一种约束方式去"要"——但记住提示词是请求，schema 是请求，**没有任何"要"的方式能替代"验"**。
- **第 7 课**：提示词可以告诉模型希望得到什么格式，**只有应用代码能够真正执行格式约束**——在进出模型的两道边界上，用运行时校验把不确定性关在业务逻辑之外。

再往后看：第 6 课借用的 function calling，在第 2 课（`02-tool-use`）里以本来面目登场（模型真的驱动你的函数执行）；这里的校验边界思想，会在 11/12 模块膨胀成整个类型系统（`types.ts` 里的 Zod schema + `superRefine` 不变量）和"伪造审批必须失败"的对抗性测试。**基础章这两课是那一切的种子。**

---

## 动手练习（按顺序做，每个都直接踩在知识点上）

1. **感受 `as` 的失明**：在第 6 课里手工构造 `const fake = JSON.parse('{"job_titel":"x"}') as JobPosting;` 然后打印 `fake.job_title`——编译通过，运行时 `undefined`。
2. **把两课缝起来**：把第 6 课的 `interface JobPosting` 改写成 `const JobPostingSchema = z.object({...})`（注意 `salary_range: z.object({...}).nullable()`、`seniority_level: z.enum([...])`），类型用 `z.infer` 推导，`as` 换成 `safeParse`。重跑 10 次循环：以前一次失败 = 程序崩溃，现在一次失败 = 打印原因继续跑。
3. **给失败样本库加菜**：往第 7 课的 `outputFailureCases` 里加一条——JSON 外面套一句"好的，以下是结果："。确认它死在第①层。
4. **修围栏问题**：在 `parseJsonObject` 之前加一步"剥掉 ```` ```json ```` 围栏"的预处理，让用例 4 起死回生。这是很多生产代码真实的第一步。
5. **合同审计**：跑几轮第 6 课，专门记录 `required_skills` 每次是否包含 TypeScript / Redis / Docker，然后**改契约消除歧义**（比如把字段拆成 `required_skills` + `bonus_skills`），再看输出是否变稳。这个练习会让你第一次体会"改提示词/schema 比改代码更能治不稳定"。
