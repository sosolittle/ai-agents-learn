# This module runs without an API key — the model decision step is mocked so
# the failure / retry / completion sequence is deterministic for learning.
#
# If you wire the loop to a real model later, this is where the key would go.
OPENAI_API_KEY=your_key_here
