# The real OpenAI API is called — a valid key is required to run this module.
# What is mocked is the tool: getOrderStatus is rigged to fail on its first
# call with a transient error and succeed on the second, so the retry path is
# exercised deterministically without depending on a flaky external service.
OPENAI_API_KEY=your_key_here
